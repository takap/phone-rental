= simple_form_for @model, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.input :name_kana
  = f.button :submit, :class => 'btn-primary'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), models_path, :class => 'btn btn-default'
