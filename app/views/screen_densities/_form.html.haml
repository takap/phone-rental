= simple_form_for @screen_density, :html => { :class => 'form-horizontal' } do |f|
  = f.input :size
  = f.button :submit, :class => 'btn-primary'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), screen_densities_path, :class => 'btn btn-default'
