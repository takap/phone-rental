- model_class = ScreenHeight
.page-header
  %h1=t '.title', :default => model_class.model_name.human.titleize

%p
  %strong= model_class.human_attribute_name(:size) + ':'
  %br
  = @screen_height.size


= link_to t('.back', :default => t("helpers.links.back")), screen_heights_path, :class => 'btn btn-default'
= link_to t('.edit', :default => t("helpers.links.edit")), edit_screen_height_path(@screen_height), :class => 'btn btn-default'
= link_to t('.destroy', :default => t("helpers.links.destroy")), screen_height_path(@screen_height), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'
