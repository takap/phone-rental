- model_class = Series
.page-header
  %h1=t '.title', :default => model_class.model_name.human.titleize

%p
  %strong= model_class.human_attribute_name(:name) + ':'
  %br
  = @series.name
%p
  %strong= model_class.human_attribute_name(:name_kana) + ':'
  %br
  = @series.name_kana


= link_to t('.back', :default => t("helpers.links.back")), series_path, :class => 'btn btn-default'
= link_to t('.edit', :default => t("helpers.links.edit")), edit_series_path(@series), :class => 'btn btn-default'
= link_to t('.destroy', :default => t("helpers.links.destroy")), series_path(@series), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'
