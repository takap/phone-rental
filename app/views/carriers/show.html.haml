- model_class = Carrier
.page-header
  %h1=t '.title', :default => model_class.model_name.human.titleize

%p
  %strong= model_class.human_attribute_name(:name) + ':'
  %br
  = @carrier.name
%p
  %strong= model_class.human_attribute_name(:name_kana) + ':'
  %br
  = @carrier.name_kana


= link_to t('.back', :default => t("helpers.links.back")), carriers_path, :class => 'btn btn-default'
= link_to t('.edit', :default => t("helpers.links.edit")), edit_carrier_path(@carrier), :class => 'btn btn-default'
= link_to t('.destroy', :default => t("helpers.links.destroy")), carrier_path(@carrier), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'
